// time-limit: 3000
/**
 * Author: distiled
 */
#include<bits/stdc++.h>
using namespace std;

#ifdef DEBUG
#include </Users/distiled/codeStuff/templates/debug.h>
#else
#define dbg(x...)
#endif
#define int            long long

signed main() {
  ios::sync_with_stdio(false); 
  cin.tie(0);
  // freopen("EQLARRY.INP", "r", stdin);
  // freopen("EQLARRY.OUT", "w", stdout);
  int tt;
  cin >> tt;
  while(tt--) {
    int n, k;
    cin >> n >> k;
    vector<int> b(n);
    for(int i = 0; i < n; i++) {
      cin >> b[i];
    }
    
  }
}


